<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:layout_marginHorizontal="12dp"
    android:layout_marginVertical="6dp"
    android:background="@drawable/bg_memo_card"
    android:padding="14dp">

    <!-- 날짜 배지 -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@drawable/bg_memo_date_badge"
        android:paddingHorizontal="10dp"
        android:paddingVertical="4dp">

        <ImageView
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:src="@drawable/ic_calendar"
            android:tint="@color/memo_date_text"
            android:contentDescription="@null" />

        <TextView
            android:id="@+id/listViewDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="4dp"
            android:textSize="13sp"
            android:textStyle="bold"
            android:textColor="@color/memo_date_text"
            tools:text="2026. 7. 16" />
    </LinearLayout>

    <!-- 메모 본문 -->
    <TextView
        android:id="@+id/listViewMemo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textSize="16sp"
        android:textColor="@color/memo_text"
        android:lineSpacingExtra="3dp"
        tools:text="오늘은 하체 운동 30분, 스쿼트 3세트 진행" />
</LinearLayout>
